# dist/ is committed on purpose: it lets users npm-install straight from a
# GitHub tarball without a build step (npm cannot reliably build globally
# installed git/tarball packages). Run `npm run build` and commit dist/
# together with src/ changes.
node_modules/
*.log
.env
.secrets
